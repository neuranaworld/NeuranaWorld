export class TerrainManager{constructor(scene){this.scene=scene;this.terrains=[];}async loadTerrainData(){const terrainFiles=['mountains','hills','plains','plateaus','rivers','lakes','valleys','wetlands','desert','forest'];for(const name of terrainFiles){const response=await fetch(`./data/terrain/${name}.txt`);if(!response.ok)continue;const text=await response.text();const data=this.parseData(text);this.createTerrainMesh(name,data);}}parseData(text){const data={};text.split(/\r?\n/).forEach(line=,value]=line.split('=');if(key
